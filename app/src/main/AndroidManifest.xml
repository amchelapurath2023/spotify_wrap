<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpotifyWrap"
        tools:targetApi="31">
        <activity
            android:name=".topArtists"
            android:exported="false"
            android:label="@string/title_activity_top_artists"
            android:theme="@style/Theme.SpotifyWrap" />
        <activity
            android:name=".Songs"
            android:exported="false"
            android:label="@string/title_activity_songs"
            android:theme="@style/Theme.SpotifyWrap" />
        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false"
            android:label="@string/title_activity_forgot_password"
            android:theme="@style/Theme.SpotifyWrap" />
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:label="@string/title_activity_email_password"
            android:theme="@style/Theme.SpotifyWrap" />
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.SpotifyWrap" />
        <activity
            android:name=".WelcomeActivity"
            android:exported="false"
            android:label="@string/title_activity_welcome"
            android:theme="@style/Theme.SpotifyWrap" />
        <activity
            android:name=".SignUpActivity"
            android:exported="false"
            android:label="@string/title_activity_sign_up"
            android:theme="@style/Theme.SpotifyWrap" />
        <activity
            android:name=".SignInActivity"
            android:exported="false"
            android:label="@string/title_activity_sign_in"
            android:theme="@style/Theme.SpotifyWrap" />
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="SettingsActivity"
            android:theme="@style/Theme.SpotifyWrap" />
        <activity
            android:name=".ResetPassword"
            android:exported="false"
            android:label="ResetPassword"
            android:theme="@style/Theme.SpotifyWrap" />
        <activity
            android:name=".DeleteAccount"
            android:exported="false"
            android:label="DeleteAccount"
            android:theme="@style/Theme.SpotifyWrap" />
        <activity
            android:name=".connectSpotifySettings"
            android:exported="false"
            android:label="connectSpotifySettings"
            android:theme="@style/Theme.SpotifyWrap" />
        <activity
            android:name=".connectSpotifyRegister"
            android:exported="false"
            android:label="connectSpotifyRegister"
            android:theme="@style/Theme.SpotifyWrap" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <!-- <data android:host="@string/host_name" android:scheme="@string/scheme_name"/> -->

            </intent-filter>
            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.VIEW"/> -->
            <!-- <category android:name="android.intent.category.DEFAULT"/> -->
            <!-- <category android:name="android.intent.category.BROWSABLE"/> -->
            <!-- <data android:host="auth" android:scheme="spotifywrap"/> -->
            <!-- </intent-filter> -->
            <!-- IF APP DOES NOT WORK REPLACE INTENT-FILTER WITH VERSION BELOW -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <data
                    android:host="auth"
                    android:scheme="spotifywrap" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WrapHistoryActivity"
            android:exported="false"
            android:label="past-wraps"
            android:theme="@style/Theme.SpotifyWrap" />
        <activity
            android:name=".playsong"
            android:exported="true" />
    </application>

</manifest>